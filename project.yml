name: BluebookHelper
targets:
  BluebookHelper:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources: [BluebookHelperApp.swift]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.th13212842200-gif.BluebookHelper
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: "" # 留空，GitHub Action 会注入
        CODE_SIGN_IDENTITY: "iPhone Distribution"
        PROVISIONING_PROFILE_SPECIFIER: ""
